import globals from 'globals';
import tseslint from 'typescript-eslint';
import js from '@eslint/js';

export default [
  {
    ignores: [
      "dist/**",
      "node_modules/**",
      "eslint.config.js",
      "vite.config.ts",
      "postcss.config.js",
      "tailwind.config.js",
    ]
  },
  js.configs.recommended, // Configuração base do ESLint
  {
    files: ["src/**/*.{js,jsx,ts,tsx}"], // Tenta encontrar todos os JS/TS/JSX/TSX em src
    languageOptions: {
      parser: tseslint.parser,
      globals: {
        ...globals.browser,
        ...globals.node,
      }
    },
    plugins: {
      '@typescript-eslint': tseslint.plugin,
    },
    rules: {
      "@typescript-eslint/no-unused-vars": "warn",
      "no-undef": "warn", // Para ver se ele pega algo
    }
  }
];
